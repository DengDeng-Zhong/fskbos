<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts PUBLIC
        "-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
        "http://struts.apache.org/dtds/struts-2.3.dtd">
<struts>
    <!-- 调试模式 -->
    <constant name="struts.devMode" value="true" />

    <package name="basicstruts2" extends="struts-default">


        <!-- 声明一个登录拦截器 -->
        <interceptors>
            <interceptor name="BOSLoginInterceptor" class="gq.dengbo.bos.web.interceptor.BOSLoginInterceptor">
                <param name="excludeMethods">login</param>
            </interceptor>
            <!-- 声明一个拦截器组 -->
            <interceptor-stack name="myDefaultStack">
                <interceptor-ref name="BOSLoginInterceptor"/>
                <interceptor-ref name="defaultStack"/>
            </interceptor-stack>
        </interceptors>

        <!-- 修改默认的拦截器 -->
        <default-interceptor-ref name="myDefaultStack"/>

        <!-- 配置全局的结果视图 -->
        <global-results>
            <result name="login">/login.jsp</result>
        </global-results>

        <!-- 配置需要进行权限控制的页面访问的规则 -->
        <action name="page_*_*">
            <result name="success" type="dispatcher">/WEB-INF/pages/{1}/{2}.jsp</result>
        </action>
        <!-- 用户模块 -->
        <action name="userAction_*" class="gq.dengbo.bos.web.action.UserAction" method="{1}">
            <result name="home">/WEB-INF/pages/common/index.jsp</result>
            <result name="loginfailure">/login.jsp</result>

        </action>

    </package>
</struts>
